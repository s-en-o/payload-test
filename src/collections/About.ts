import { GlobalConfig } from 'payload/types';

const about: GlobalConfig = {
    slug: 'about',
    fields: [
        {
            name: 'content',
            type: 'richText',
        },
    ],
};

export default about;
